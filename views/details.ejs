<div class="container mt-3">
    <div class="row">
        <div class="col-md-8">
            <h2>Details Contacts!</h2>
            <% if (!contact) { %>
            <div class="alert alert-warning" role="alert">
                Contact yang anda cari tidak ditemukan, silahkan kembali ke <a href="/contact"
                    class="alert-link">halaman contact</a>. dan cari nama yang tersedia
            </div>
            <% } else { %>
            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title"><%= contact.nama %></h5>
                    <h6 class="card-subtitle mb-2 text-muted"><%= contact.nohp %></h6>
                    <p class="card-text"><%= contact.email %></p>
                    <a href="/contact" class="card-link"><i class="bi bi-arrow-left-circle-fill"></i> back</a>
                </div>
            </div>
            <% } %>
        </div>
    </div>
</div>